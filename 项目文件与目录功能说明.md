# fast_try 项目文件与目录功能说明

## 1. 项目总览

本项目是一个前后端分离的“社区志愿服务系统”。

- 后端技术栈：`Django + Django REST Framework + JWT + SQLite`
- 前端技术栈：`Vue3 + TypeScript + Vite + Element Plus + Pinia`
- 启停脚本：根目录 `run_all.bat` 与 `stop_all.bat`

---

## 2. 根目录文件

| 文件/目录 | 功能说明 |
|---|---|
| `.git/` | Git 仓库元数据目录。 |
| `backend/` | Django 后端代码、数据库和媒体资源。 |
| `frontend/` | Vue 前端工程代码。 |
| `scripts/` | 运维辅助脚本（如获取本机 IP）。 |
| `run_all.bat` | 一键启动脚本：迁移数据库、检查依赖、分别启动后端 8000 和前端 5173。 |
| `stop_all.bat` | 一键停止脚本：按窗口标题和端口（8000/5173）关闭相关进程。 |

---

## 3. scripts 目录

| 文件 | 功能说明 |
|---|---|
| `scripts/get_local_ip.ps1` | 获取当前机器可用于局域网访问的 IPv4 地址，供启动脚本显示 LAN 访问地址。 |

---

## 4. backend 目录

## 4.1 backend 根级文件

| 文件/目录 | 功能说明 |
|---|---|
| `backend/manage.py` | Django 管理入口，支持 `runserver`、`migrate`、`shell` 等命令。 |
| `backend/db.sqlite3` | 当前开发环境 SQLite 数据库文件。 |
| `backend/create_db.sql` | 旧 MySQL 建库脚本（当前项目实际默认使用 SQLite）。 |
| `backend/volunteer_system/` | Django 项目配置目录。 |
| `backend/api/` | 核心业务应用（模型、序列化器、视图、路由、迁移）。 |
| `backend/media/` | 用户头像、社区封面、活动封面等上传/示例图片。 |

## 4.2 backend/volunteer_system

| 文件 | 功能说明 |
|---|---|
| `backend/volunteer_system/__init__.py` | Python 包初始化文件。 |
| `backend/volunteer_system/settings.py` | 全局配置：数据库、已安装应用、JWT、CORS、媒体目录、自定义用户模型等。 |
| `backend/volunteer_system/urls.py` | 根路由：挂载 `admin/` 和 `api/`。 |
| `backend/volunteer_system/asgi.py` | ASGI 部署入口。 |
| `backend/volunteer_system/wsgi.py` | WSGI 部署入口。 |
| `backend/volunteer_system/__pycache__/...` | Python 字节码缓存文件，自动生成。 |

## 4.3 backend/api 核心文件

| 文件 | 功能说明 |
|---|---|
| `backend/api/__init__.py` | Python 包初始化文件。 |
| `backend/api/apps.py` | Django App 配置（`ApiConfig`）。 |
| `backend/api/models.py` | 数据模型定义：用户、社区、活动、报名、签到工时、公告、评价、评论、操作日志等。 |
| `backend/api/serializers.py` | 序列化与字段校验规则：注册校验、活动时间校验、评分范围、评论校验等。 |
| `backend/api/views.py` | 业务接口核心：认证登录、权限范围、审核流、状态流转、签到签退、统计分析、日志记录。 |
| `backend/api/urls.py` | DRF 路由注册：`users/communities/activities/registrations/attendances/notices/stats` 等。 |
| `backend/api/permissions.py` | 角色权限类定义（系统管理员、社区管理员、志愿者等）。 |
| `backend/api/admin.py` | Django Admin 后台模型注册与列表展示配置。 |
| `backend/api/tests.py` | 测试入口文件（目前为默认空模板，尚未补充自动化测试）。 |
| `backend/api/__pycache__/...` | Python 字节码缓存文件，自动生成。 |

## 4.4 backend/api/migrations 迁移文件

| 文件 | 功能说明 |
|---|---|
| `backend/api/migrations/__init__.py` | 迁移包初始化文件。 |
| `backend/api/migrations/0001_initial.py` | 初始建表迁移，创建基础业务模型。 |
| `backend/api/migrations/0002_alter_activity_options_alter_attendance_options_and_more.py` | 增补多模型字段、约束和选项，完善早期结构。 |
| `backend/api/migrations/0003_user_approval_note_user_approval_status_and_more.py` | 引入用户审核相关字段（如社区管理员审核状态/备注等）。 |
| `backend/api/migrations/0004_activitytype_activity_other_type.py` | 引入活动类型模型与“其他类型补充”字段。 |
| `backend/api/migrations/0005_seed_activity_types.py` | 初始化活动类型种子数据（环境保护、敬老助残等）。 |
| `backend/api/migrations/0006_activitycomment.py` | 新增活动评论模型。 |
| `backend/api/migrations/0007_activity_type_fk_constraints_indexes.py` | 将活动类型从文本迁移到外键，并补充索引/约束。 |
| `backend/api/migrations/0008_attendance_hours_one_decimal.py` | 工时字段规范为 1 位小数并做历史数据归一化。 |
| `backend/api/migrations/0009_activity_review_status_and_lifecycle_status.py` | 活动审核状态与生命周期状态调整及迁移。 |
| `backend/api/migrations/0010_communitychangerequest.py` | 新增志愿者社区变更申请模型。 |
| `backend/api/migrations/0011_remove_communitychangerequest_effective_at.py` | 移除社区变更中的 `effective_at` 字段。 |
| `backend/api/migrations/0012_operationlog.py` | 新增系统管理员操作日志模型。 |
| `backend/api/migrations/__pycache__/...` | 迁移模块字节码缓存文件，自动生成。 |

## 4.5 backend/media 资源文件

### 4.5.1 社区封面

| 文件 | 功能说明 |
|---|---|
| `backend/media/communities/阳光社区.webp` | 社区示例封面图。 |
| `backend/media/communities/翻斗花园.webp` | 社区示例封面图。 |
| `backend/media/communities/清泉社区.webp` | 社区示例封面图。 |

### 4.5.2 用户头像

| 文件 | 功能说明 |
|---|---|
| `backend/media/avatars/下载_12.png` | 用户示例头像。 |
| `backend/media/avatars/下载_8.png` | 用户示例头像。 |
| `backend/media/avatars/下载_7.png` | 用户示例头像。 |
| `backend/media/avatars/下载_40.png` | 用户示例头像。 |
| `backend/media/avatars/下载_2.png` | 用户示例头像。 |

### 4.5.3 活动封面

| 文件 | 功能说明 |
|---|---|
| `backend/media/activities/老年公益义诊.png` | 活动示例封面图。 |

---

## 5. frontend 目录

## 5.1 frontend 根级配置

| 文件/目录 | 功能说明 |
|---|---|
| `frontend/package.json` | 前端依赖与脚本（`dev/build/lint`）。 |
| `frontend/package-lock.json` | 依赖锁定文件，确保安装版本一致。 |
| `frontend/README.md` | Vue 模板默认说明文档。 |
| `frontend/vite.config.ts` | Vite 配置（开发端口、代理 `/api` `/media`、路径别名 `@`）。 |
| `frontend/eslint.config.ts` | ESLint + Vue + TypeScript + Oxlint 配置。 |
| `frontend/tsconfig.json` | TS 工程引用入口（引用 app/node 两套 tsconfig）。 |
| `frontend/tsconfig.app.json` | 浏览器端 TS 编译配置。 |
| `frontend/tsconfig.node.json` | Node 侧配置文件（Vite、ESLint 配置文件类型检查）。 |
| `frontend/env.d.ts` | Vite 环境类型声明。 |
| `frontend/index.html` | 前端 HTML 宿主页面，挂载 `#app`。 |
| `frontend/public/favicon.ico` | 网站图标。 |
| `frontend/src/` | 前端源代码目录。 |

## 5.2 frontend/src 根文件

| 文件 | 功能说明 |
|---|---|
| `frontend/src/main.ts` | 前端启动入口，注册 Pinia、Router、Element Plus。 |
| `frontend/src/App.vue` | 顶层组件，仅承载路由出口 `RouterView`。 |

## 5.3 frontend/src/assets

| 文件 | 功能说明 |
|---|---|
| `frontend/src/assets/base.css` | 全局设计变量（颜色、圆角、阴影、字体）和基础样式。 |
| `frontend/src/assets/main.css` | 页面通用样式、Element Plus 主题变量与布局风格。 |
| `frontend/src/assets/logo.svg` | Vue 模板默认 Logo 资源（业务中未核心使用）。 |

## 5.4 frontend/src/router

| 文件 | 功能说明 |
|---|---|
| `frontend/src/router/index.ts` | 路由表与路由守卫：登录态校验、角色访问控制、默认跳转策略。 |

## 5.5 frontend/src/stores

| 文件 | 功能说明 |
|---|---|
| `frontend/src/stores/user.ts` | 用户状态管理：登录、注册、拉取用户信息、更新资料、登出。 |
| `frontend/src/stores/counter.ts` | Vue 模板默认计数器 store（示例遗留，业务未使用）。 |

## 5.6 frontend/src/utils

| 文件 | 功能说明 |
|---|---|
| `frontend/src/utils/request.ts` | Axios 实例封装：JWT 注入、401 处理、媒体 URL 归一化、列表兼容处理。 |

## 5.7 frontend/src/types

| 文件 | 功能说明 |
|---|---|
| `frontend/src/types/index.ts` | 前端核心类型定义：用户、活动、报名、工时、公告、统计、日志等。 |

## 5.8 frontend/src/composables

| 文件 | 功能说明 |
|---|---|
| `frontend/src/composables/useIsMobile.ts` | 响应式移动端判断 Hook（基于 `matchMedia`）。 |

## 5.9 frontend/src/views（用户与公共页面）

| 文件 | 功能说明 |
|---|---|
| `frontend/src/views/LoginView.vue` | 登录页。 |
| `frontend/src/views/RegisterView.vue` | 注册页（志愿者/社区管理员，社区管理员需审核）。 |
| `frontend/src/views/HomeView.vue` | 主框架布局页（侧边栏、顶部栏、退出、首次资料完善弹窗）。 |
| `frontend/src/views/DashboardView.vue` | 仪表盘页：概览卡片、服务时长排行、社区分布。 |
| `frontend/src/views/ActivityListView.vue` | 活动列表页（搜索、状态筛选、卡片/表格展示）。 |
| `frontend/src/views/ActivityDetailView.vue` | 活动详情页（报名、评论、管理员开始/结束活动）。 |
| `frontend/src/views/MyRegistrationsView.vue` | 志愿者报名记录页（取消报名、签到、签退、工时状态查看）。 |
| `frontend/src/views/NoticeListView.vue` | 公告列表页。 |
| `frontend/src/views/ProfileView.vue` | 个人中心（资料编辑、头像上传、社区变更申请、志愿者统计卡片）。 |
| `frontend/src/views/AboutView.vue` | 简单“关于系统”页面（当前未在主路由中使用）。 |

## 5.10 frontend/src/views/admin（管理端页面）

| 文件 | 功能说明 |
|---|---|
| `frontend/src/views/admin/ActivityManageView.vue` | 活动管理：新增/编辑/删除活动，手动开始/结束。 |
| `frontend/src/views/admin/ReviewManageView.vue` | 审核中心：管理员账号审核、活动审核、社区变更审核、报名审核、工时审核。 |
| `frontend/src/views/admin/NoticeManageView.vue` | 公告管理：发布、编辑、删除，支持全局/社区范围。 |
| `frontend/src/views/admin/UserManageView.vue` | 用户管理：新增/编辑/删除用户，角色与状态维护。 |
| `frontend/src/views/admin/CommunityManageView.vue` | 社区管理：新增/编辑/删除社区与封面。 |
| `frontend/src/views/admin/ActivityTypeManageView.vue` | 活动类型管理：启停、排序、说明维护。 |
| `frontend/src/views/admin/OperationLogView.vue` | 操作日志查询页：按模块/动作/状态/关键字过滤并查看详情。 |

## 5.11 frontend/src/components（模板遗留组件）

| 文件 | 功能说明 |
|---|---|
| `frontend/src/components/HelloWorld.vue` | Vue 脚手架默认示例组件（业务未使用）。 |
| `frontend/src/components/TheWelcome.vue` | Vue 脚手架默认欢迎组件（业务未使用）。 |
| `frontend/src/components/WelcomeItem.vue` | 脚手架欢迎页子组件（业务未使用）。 |

## 5.12 frontend/src/components/icons（模板遗留图标）

| 文件 | 功能说明 |
|---|---|
| `frontend/src/components/icons/IconCommunity.vue` | 脚手架示例 SVG 图标。 |
| `frontend/src/components/icons/IconDocumentation.vue` | 脚手架示例 SVG 图标。 |
| `frontend/src/components/icons/IconEcosystem.vue` | 脚手架示例 SVG 图标。 |
| `frontend/src/components/icons/IconSupport.vue` | 脚手架示例 SVG 图标。 |
| `frontend/src/components/icons/IconTooling.vue` | 脚手架示例 SVG 图标。 |

---

## 6. 自动生成文件与说明

| 路径 | 说明 |
|---|---|
| `backend/**/__pycache__/` | Python 自动生成的字节码缓存，删除后会自动重新生成。 |
| `backend/api/migrations/__pycache__/` | Django 迁移模块的字节码缓存。 |
| `frontend/package-lock.json` | npm 自动维护的依赖锁文件，不建议手改。 |

---

## 7. 建议阅读顺序（快速理解项目）

| 顺序 | 文件 |
|---|---|
| 1 | `backend/api/models.py`（先看业务实体） |
| 2 | `backend/api/serializers.py`（再看校验规则） |
| 3 | `backend/api/views.py`（看核心业务流程） |
| 4 | `backend/api/urls.py`（看接口总入口） |
| 5 | `frontend/src/router/index.ts`（看前端权限路由） |
| 6 | `frontend/src/stores/user.ts` 与 `frontend/src/utils/request.ts`（登录态与请求） |
| 7 | `frontend/src/views/admin/ReviewManageView.vue`（集中理解审核流程） |

